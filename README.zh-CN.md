[English](README.md) | [简体中文](README.zh-CN.md)

# 此刻 · Here

**不必等到更强大。**

此刻是一个面向性骚扰与性暴力经历者的全球支持工作台前端原型，核心理念是：不必等自己准备好了，才开始维权。用户无需先整理好情绪、完整回忆经历或独自弄懂程序。行动工作台复用零散记录、协助准备咨询材料，将披露与提交的决定权留给用户。

当前演示支持准备工作，不会真实报案。当地规则、时间要求、真实提交与跟进仍待接入。

界面默认英语，可切换简体中文。适配手机与桌面，无依赖、无构建步骤。

### 功能

- 四个页面：从这里开始、寻找支持、记录我的经历、我的下一步。
- 当前所在地与事件发生地分别填写。地区用于筛选已收录资源，也可以独立选择资源检索地区。
- 支持类别筛选、中英文关键词搜索、加载状态与空结果提示。
- 当前会话记录、清除前确认与文本下载。
- 三种草稿目标：了解支持与咨询、准备投诉或报案咨询、申请生活安排调整。
- 可编辑的双语草稿模板，以及核对后的模拟提交结果预览。
- 隐私说明、语义导航、键盘焦点与小猫 Logo。

### 本地预览

直接打开 `index.html`，或使用任意静态文件服务器运行此目录。页面使用 URL hash 导航，资源路径均为相对路径，适用于 GitHub Pages 项目子路径。

### 发布到 GitHub Pages

1. 将 `index.html`、`styles.css`、`i18n.js`、`app.js`、`mock-api.js`、`resources.js`、`logo-cat.png` 和 `.nojekyll` 放在仓库根目录。
2. 打开 **Settings → Pages → Build and deployment**，选择 **Deploy from a branch**。
3. 选择 `main` 分支与 `/ (root)`，保存。
4. 等待部署完成，打开 GitHub Pages 提供的地址。对于 `erinirex/here` 仓库，启用 Pages 且部署成功后的项目地址为 `https://erinirex.github.io/here/`。

### 项目文件

| 文件 | 用途 |
| --- | --- |
| `index.html` | 页面框架、导航与弹窗 |
| `styles.css` | 视觉样式与响应式布局 |
| `app.js` | 页面、内存状态、表单与下载 |
| `i18n.js` | 英语与简体中文界面文案 |
| `mock-api.js` | 目录搜索适配、双语草稿模板与模拟提交 |
| `logo-cat.png` | 小猫 Logo 与浏览器图标 |
| `.nojekyll` | 静态托管时禁用 Jekyll 处理 |

### 接入真实 API

`mock-api.js` 提供 `searchResources`、`prepareDraft`、`simulateSubmission` 三个异步方法，目前完全在浏览器中运行，不发起后端请求。

接入真实服务时使用后端代理，不要将 API 密钥写入前端文件或仓库。替换模拟提交前，应实现接收方选择、资料披露范围控制、明确授权、重复提交保护与准确的回执状态。资源需独立核验来源、当地适用性和有效期。目录链接至真实机构，但不判断法律适用资格或医疗适合性。

### 语言行为

切换语言会保留表单内容，不会自动翻译原始记录或已编辑的草稿。新草稿使用开始生成时的界面语言。如需另一种语言的模板，可切换后重新生成；重新生成会替换现有草稿及其修改。

语言偏好不持久化，刷新后恢复英语。

### 隐私与限制

输入仅保留在 JavaScript 内存中。应用不使用 localStorage、Cookie、数据库、分析追踪或网络上传来保存输入。刷新或关闭页面会清除会话记录。用户主动下载的文件会保留在设备上。托管平台仍可能记录普通访问日志。

资源卡片链接至已核对的政府或机构来源。搜索在本地筛选静态目录；草稿生成与提交仍为模拟。没有真实 AI、翻译服务、账户系统、附件上传、紧急呼叫或机构对接。本原型不用于处理真实个案或提供紧急救援。

支持 `document.modelContext` 的浏览器可以提供页面内导航工具，不暴露读取私人记录或提交的工具；其他浏览器可正常使用。

## 地区资源目录

`resources.js` 现收录 19 项经官网核对的资源，覆盖 8 个地区并包含全球目录入口。核对日期为 2026-09-23，是非穷尽静态目录，不是实时搜索 API 或服务可用性验证。可按当前所在地或事件发生地、类别及中英文关键词筛选，检索不传输事件记录。外部链接有独立隐私政策；草稿和提交仍为模拟。
