import fs from 'node:fs';
const base=new URL('../',import.meta.url);const raw=process.argv[2];let u;try{u=new URL(raw);}catch{throw Error('Provide the deployed HTTPS Worker URL');}if(u.protocol!=='https:'||u.username||u.password||u.search||u.hash||u.pathname!=='/')throw Error('Use only the HTTPS Worker origin');
const origin=u.origin;
fs.writeFileSync(new URL('analytics-config.js',base),'window.HERE_ANALYTICS_ENDPOINT = '+JSON.stringify(origin)+';\n');
for(const name of ['README.md','README.zh-CN.md']){const p=new URL(name,base);let s=fs.readFileSync(p,'utf8');const block='<!-- metrics:start -->\n[![Page views]('+origin+'/badge/views)](https://erinirex.github.io/here/stats.html) [![Visitors today UTC]('+origin+'/badge/visitors)](https://erinirex.github.io/here/stats.html) [![Clicks]('+origin+'/badge/clicks)](https://erinirex.github.io/here/stats.html)\n\n[Live statistics / 实时统计](https://erinirex.github.io/here/stats.html) · Opted-in traffic only / 仅统计同意参与的访问\n<!-- metrics:end -->';s=s.replace(/<!-- metrics:start -->[\s\S]*?<!-- metrics:end -->/,()=>block);fs.writeFileSync(p,s);}console.log('Frontend endpoint and README badges configured. Publish the website changes.');
